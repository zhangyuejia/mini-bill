<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minibill.bus.mapper.BusFamilyMemberMapper">

    <select id="isFamilyMember" resultType="boolean">
        SELECT COUNT(*) > 0 FROM bus_family_member
        WHERE family_id = #{familyId} AND user_id = #{userId} AND status = 0
    </select>

</mapper>
